services:
  bluos-mock-server:
    image: bluos-mock-server
    build: .
    network_mode: host   # required for mDNS multicast to reach the LAN
    environment:
      - API_DELAY_MS=0
      - MDNS_ENABLED=1
